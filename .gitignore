*.iml
.gradle
/local.properties
/.idea/
.DS_Store
/build
/captures
.externalNativeBuild
.cxx
local.properties
# Whisper models are ~57 MB each and are downloaded by the app on first launch
# (Config.MODEL_DOWNLOAD_URL), never committed.
/models
*.bin
